# Required Home Assistant configuration
HA_URL=http://homeassistant.local:8123
HA_TOKEN=your_long_lived_access_token_here

# Anthropic API configuration (optional)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Model configuration (optional, these are the defaults)
DEFAULT_LOCAL_MODEL=llama3
DEFAULT_CLOUD_MODEL=claude-3-opus

# Ollama configuration (optional)
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Set to true to use a mock model instead of Ollama (useful in environments where Ollama isn't available)
# MOCK_LOCAL_MODEL=true

# Logging configuration (optional)
# Uncomment to enable debug logging
# LOG_LEVEL=DEBUG 